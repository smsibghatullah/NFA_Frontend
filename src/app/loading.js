import React from "react";
import LoadingScreen from "./components/main/LoadingScreen"; // Import the loading screen

const Loading = () => {
  return <LoadingScreen />;
};

export default Loading;